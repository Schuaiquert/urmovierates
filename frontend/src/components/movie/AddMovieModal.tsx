'use client';

import { Modal } from '@/components/common/Modal';

interface Props {
  open: boolean;
  onClose: () => void;
  onAdded: () => void;
}

export function AddMovieModal(_props: Props) {
  return null;
}
